# コンテキストリセット（長期実装）ルール

## リセットの目安

以下のいずれかに該当したらセッション分割を検討する：

- 実装が 3 スプリント（3 機能）を超える見込みのとき
- コンテキスト使用率が 70% を超えたとき

## セッション分割の手順

**分割前**: 完了内容・残タスクをリストアップし、`/verify` で成果を確認してからセッションを終える。

**分割後**: 前セッションの完了内容・残タスクを冒頭で確認し、スプリントコントラクトを再宣言して実装開始。
