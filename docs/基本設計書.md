# DevNext 基本設計書

| 項目 | 内容 |
|------|------|
| ドキュメント名 | DevNext 基本設計書 |
| バージョン | 1.0 |
| 作成日 | 2026-03-23 |
| 対象システム | DevNext Web アプリケーション |

---

## 目次

1. [システム概要](#1-システム概要)
2. [目的・背景](#2-目的背景)
3. [システム構成](#3-システム構成)
4. [技術スタック](#4-技術スタック)
5. [アーキテクチャ概要](#5-アーキテクチャ概要)
6. [機能一覧](#6-機能一覧)
7. [画面一覧](#7-画面一覧)
8. [非機能要件](#8-非機能要件)
9. [環境構成](#9-環境構成)
10. [外部インターフェース](#10-外部インターフェース)

---

## 1. システム概要

DevNext は、ASP.NET Core 10 MVC をベースとした **エンタープライズ向け Web アプリケーションテンプレート**である。
旧 .NET Framework 版（DevNet）からの移行・リニューアルプロジェクトとして設計されており、業務案件で共通して必要となる機能を一通り実装した再利用可能な基盤を提供する。

### システム名

| 項目 | 内容 |
|------|------|
| システム名 | DevNext |
| システム種別 | Web アプリケーション（MVC） |
| 稼働環境 | Windows / Linux（.NET 10.0） |
| データベース | SQL Server |
| 想定利用者 | 認証済みユーザー（Admin / Member） |

---

## 2. 目的・背景

### 背景

実際の業務案件で得た知見をもとに、汎用的に再利用できる Web アプリケーション基盤として整理したもの。
「次の案件でもそのまま使える」ことを意識し、認証・CRUD・共通ライブラリなど、どの案件でも必要になる機能を一通り組み込んでいる。

### 目的

- 業務 Web アプリケーション開発の初期コスト削減
- 共通機能（認証・CRUD・ファイル管理・帳票出力）の標準化
- ASP.NET Core 10 の最新構成をベースにした保守性の高いコードベース提供
- CommonLibrary による横断的機能の再利用促進

### 設計方針

- **案件ごとの拡張を前提とした設計**：特定のビジネスロジックに依存しない構造
- **CommonLibrary の切り出し**：アプリ本体と共通機能を分離し、別プロジェクトへの流用を容易化
- **保守性の優先**：リポジトリ・サービス層・ViewModel を明確に分離し、影響範囲を局所化
- **テスト可能な設計**：DI・モックを最初から意識した構造化

---

## 3. システム構成

### プロジェクト構成

```
DevNext/
├── DevNext/              # メイン Web アプリ（RootNamespace: Site）
├── CommonLibrary/        # 共通ライブラリ（RootNamespace: Dev.CommonLibrary）
├── BatchSample/          # バッチ処理サンプル（コンソールアプリ）
├── Tests/                # ユニットテスト（xUnit）
└── DevNext.slnx          # ソリューションファイル
```

### プロジェクト間依存関係

```
DevNext (Web)
    └── CommonLibrary

BatchSample
    └── CommonLibrary

Tests
    └── DevNext (Web)
    └── CommonLibrary
```

---

## 4. 技術スタック

### フレームワーク・ランタイム

| カテゴリ | ライブラリ / 技術 | バージョン |
|----------|------------------|-----------|
| ランタイム | .NET | 10.0 |
| Web フレームワーク | ASP.NET Core MVC | 10.0 |
| ORM | Entity Framework Core | 10.0.0 |
| データベースドライバ | Microsoft.EntityFrameworkCore.SqlServer | 10.0.0 |
| 認証・認可 | ASP.NET Core Identity | 10.0.0 |

### 主要ライブラリ

| カテゴリ | ライブラリ | バージョン |
|----------|-----------|-----------|
| オブジェクトマッピング | AutoMapper | 16.1.1 |
| Excel 処理 | ClosedXML（MIT ライセンス、商用利用無償） | 0.104.2 |
| CSV 処理 | CsvHelper | 33.0.1 |
| PDF 生成 | Microsoft.Playwright | 1.59.0 |
| JSON | Newtonsoft.Json | 13.0.3 |

### テスト

| カテゴリ | ライブラリ | バージョン |
|----------|-----------|-----------|
| テストフレームワーク | xUnit | 2.9.3 |
| モックライブラリ | Moq | 4.20.72 |

### フロントエンド

| カテゴリ | ライブラリ | バージョン |
|----------|-----------|-----------|
| CSS フレームワーク | Bootstrap | 5.3.8 |
| JavaScript | jQuery | - |
| バリデーション | jQuery Validation | - |
| グラフ描画 | Chart.js | 4.4.7 |
| カレンダー UI | FullCalendar | 6.1.15 |

---

## 5. アーキテクチャ概要

### レイヤードアーキテクチャ

本システムは以下の 5 層構造を採用する。

```
┌─────────────────────────────────────────────────────┐
│  Presentation Layer（プレゼンテーション層）             │
│  Controllers / Views / ViewModels                    │
├─────────────────────────────────────────────────────┤
│  Business Logic Layer（ビジネスロジック層）             │
│  Services                                            │
├─────────────────────────────────────────────────────┤
│  Data Access Layer（データアクセス層）                  │
│  Repositories                                        │
├─────────────────────────────────────────────────────┤
│  Domain Layer（ドメイン層）                            │
│  Entities                                            │
├─────────────────────────────────────────────────────┤
│  Cross-cutting Concerns（横断的関心事）                 │
│  CommonLibrary（Logger, Attributes, Extensions等）   │
└─────────────────────────────────────────────────────┘
```

### リクエスト処理フロー

```
クライアント
    │
    ▼
[HTTPS Redirect]
    │
    ▼
[Static Files]
    │
    ▼
[Routing]
    │
    ▼
[Session]
    │
    ▼
[Authentication / Authorization]
    │
    ▼
[Controller] ─── [AccessLogAttribute]
    │
    ▼
[Service]
    │
    ▼
[Repository]
    │
    ▼
[DBContext (Entity Framework Core)]
    │
    ▼
[SQL Server]
```

### 主なデザインパターン

| パターン | 適用箇所 |
|----------|---------|
| Repository パターン | データアクセス層（RepositoryBase + 各 Repository） |
| Service Layer パターン | ビジネスロジック層（各 Service） |
| Dependency Injection | Program.cs での DI コンテナ登録 |
| AutoMapper | Entity ↔ ViewModel の変換 |
| Soft Delete | DelFlag による論理削除 |
| Generic Programming | RepositoryBase\<TEntity, TCondModel\> |
| Filter / Middleware | アクセスログ（AccessLogAttribute） |

---

## 6. 機能一覧

### 認証・アカウント管理

| 機能 | 概要 | 権限 |
|------|------|------|
| ログイン | メールアドレス・パスワードによる認証 | 全員 |
| ログアウト | セッション破棄 | 認証済み |
| ユーザー登録 | 新規アカウント作成 | 全員 |
| パスワードリセット | メールによるパスワード再設定 | 全員 |
| パスワード変更 | ログイン済みユーザーによる変更 | 認証済み |
| アカウントロック | 5 回連続失敗で 5 分間ロック | - |

**パスワードポリシー**

| 条件 | 設定値 |
|------|--------|
| 最低文字数 | 6 文字 |
| 大文字 | 必須 |
| 小文字 | 必須 |
| 数字 | 必須 |
| 記号 | 必須 |

### DB サンプル（CRUD）

| 機能 | 概要 |
|------|------|
| 一覧表示 | 検索・ページング・ソート対応 |
| 新規作成 | 親エンティティの単体作成 |
| 編集 | 既存データの更新 |
| 削除 | 論理削除（DelFlag = true） |
| 一括登録 | 親＋子エンティティをまとめて登録 |
| 一括編集 | 親＋子エンティティをまとめて編集（子行の動的追加・削除） |
| 子エンティティ CRUD | 詳細画面から子の追加・編集・削除 |
| ファイルアップロード | エンティティへのファイル添付 |
| Excel インポート | XLSX ファイルからの一括登録 |
| Excel エクスポート | 一覧・明細の Excel 出力 |
| PDF エクスポート（一覧） | 一覧の PDF 出力 |
| PDF エクスポート（単体） | 明細 PDF（子エンティティ一覧・画像埋め込み対応） |
| 検索条件の復帰 | 編集・削除後に一覧へ戻る際に検索条件・ページ位置を再現 |

### ファイル管理

| 機能 | 概要 |
|------|------|
| アップロード | サイズ・拡張子バリデーション付きアップロード |
| 一覧表示 | 検索・ページング |
| ダウンロード | ファイルのダウンロード |
| 削除 | ファイル削除（物理 + メタ情報） |

### メール送信サンプル

| 機能 | 概要 |
|------|------|
| メール送信 | テンプレートベースのメール送信 |
| プレースホルダー置換 | テンプレート内の変数を動的に置換 |
| 送信ログ記録 | 送信結果（成功/失敗・エラー詳細）を DB に保存 |
| 送信ログ一覧 | 送信履歴の検索・ページング表示 |

### Excel サンプル（`Samples/ExcelSample` — 独立プロジェクト）

> ClosedXML を使った Excel エクスポート・インポートのサンプル。コア DevNext とは独立して起動する。

| 機能 | 概要 |
|------|------|
| 商品一覧 | 登録済み商品の全件表示 |
| Excel エクスポート | 全件をスタイリング付き XLSX でダウンロード（ゼブラ・列幅自動調整） |
| Excel インポート | XLSX アップロードによる一括登録（バリデーション・行番号付きエラー表示） |
| 削除 | 論理削除（DelFlag = true） |

### REST API サンプル（`Samples/ApiSample` — 独立プロジェクト）

> ASP.NET Core 10 Web API + JWT Bearer 認証 + Swagger UI のサンプル。コア DevNext とは独立して起動する。
> Swagger UI は `/swagger` でアクセス可能。

| 機能 | 概要 |
|------|------|
| 商品 CRUD API | GET /api/items（一覧）、GET /api/items/{id}（詳細）、POST/PUT/DELETE（登録・更新・削除） |
| JWT 認証 | POST /api/auth/login でメール・パスワードを検証し JWT トークンを発行 |
| ロール認可 | 認証済みユーザーは参照可。Admin ロールのみ登録・更新・削除を実行可能（403 制御） |
| Swagger UI | `/swagger` で全エンドポイントを確認・実行可能（JWT トークン入力対応） |
| バリデーション | DataAnnotations によるリクエスト検証（400 + 日本語エラーメッセージ） |

### 多段階フォーム（ウィザード）

| 機能 | 概要 |
|------|------|
| Step 1 | 基本情報入力（氏名・メール・電話番号） |
| Step 2 | 詳細情報入力（件名・内容・カテゴリ・希望日） |
| 確認画面 | 入力内容の確認 |
| 完了画面 | 登録完了・DB 保存 |

### ダッシュボード

| 機能 | 概要 |
|------|------|
| サマリーカード表示 | 各エンティティの総件数をカード形式で表示 |
| グラフ表示 | Chart.js による棒・折れ線・ドーナツ・横棒グラフ（5種） |
| リアルタイム更新 | Ajax ポーリング（30秒間隔）でグラフデータを自動更新 |

### 承認ワークフロー

| 機能 | 概要 | 権限 |
|------|------|------|
| 申請一覧 | 検索・ページング（Admin は全件、Member は自分の申請のみ） | 認証済み |
| 申請作成 | 下書き保存 / 即時申請（Draft → Pending） | 認証済み |
| 申請編集 | 自分の Draft のみ編集可能 | 認証済み |
| 申請詳細 | 申請内容・ステータス・コメント表示 | 認証済み |
| 申請削除 | 自分の Draft のみ削除可能 | 認証済み |
| 承認・却下 | Pending → Approved / Rejected（コメント入力可） | Admin |
| 通知 | 申請時は Admin 全員、承認・却下時は申請者に通知（ベルアイコン） | 認証済み |
| CSV エクスポート | 現在の検索条件で全件を CSV（BOM 付き UTF-8）でダウンロード | 認証済み |
| Excel エクスポート | 現在の検索条件で全件を XLSX でダウンロード（ClosedXML 使用） | 認証済み |

### スケジュール・カレンダー

| 機能 | 概要 | 権限 |
|------|------|------|
| カレンダー表示 | FullCalendar.js による月・週・日ビュー切り替え | 認証済み |
| 予定一覧取得 | 個人予定・共有予定・招待された予定を色分け表示 | 認証済み |
| 予定作成 | 件名・開始/終了・終日・共有・繰り返し・参加者を設定 | 認証済み |
| 予定編集 | 自分が作成した予定のみ編集可能 | 認証済み |
| 予定削除 | 自分が作成した予定のみ削除可能（論理削除） | 認証済み |
| 繰り返し設定 | 毎日・毎週（曜日指定）・毎月、間隔・終了日指定 | 認証済み |
| 参加者招待 | 共有予定に他ユーザーを招待（承諾/辞退の返答管理） | 認証済み |
| 参加ステータス更新 | 招待された予定に対して承諾・辞退を返答 | 認証済み |

### ユーザー・ロール管理（Admin 限定）

| 機能 | 概要 |
|------|------|
| ユーザー一覧 | 検索・ページング |
| ユーザー情報編集 | メールアドレス・ユーザー名の変更 |
| ユーザー削除 | 初期 Admin ユーザーは削除不可 |
| ロール付与・剥奪 | Admin / Member ロールの管理 |

### 共通機能（CommonLibrary）

| 機能 | 概要 |
|------|------|
| ジェネリックリポジトリ | CRUD・バッチ挿入・論理削除・履歴管理 |
| アクセスログ | コントローラーへのアクセスを記録 |
| バッチサービス | Mutex による多重起動防止 |
| ページング・ソート | 汎用ページングユーティリティ |
| ファイル検証属性 | サイズ・拡張子の検証 |
| Enum ユーティリティ | 表示名取得・ドロップダウン生成 |

---

## 7. 画面一覧

| 画面 ID | 画面名 | URL | 権限 |
|--------|--------|-----|------|
| ACC-001 | ログイン画面 | /Account/Login | 全員 |
| ACC-002 | ユーザー登録画面 | /Account/Register | 全員 |
| ACC-003 | パスワードリセット申請 | /Account/ForgotPassword | 全員 |
| ACC-004 | パスワードリセット | /Account/ResetPassword | 全員 |
| MNG-001 | パスワード変更画面 | /Manage/ChangePassword | 認証済み |
| HOM-001 | ホーム（ダッシュボード） | /Home/Index | 認証済み |
| DB-001 | DB サンプル一覧 | /DatabaseSample/Index | 認証済み |
| DB-002 | DB サンプル詳細 | /DatabaseSample/Details | 認証済み |
| DB-003 | DB サンプル作成・編集 | /DatabaseSample/Edit | 認証済み |
| DB-004 | DB サンプル一括登録・編集 | /DatabaseSample/BulkEdit | 認証済み |
| DB-005 | DB サンプル削除確認 | /DatabaseSample/Delete | 認証済み |
| DB-006 | Excel インポート | /DatabaseSample/ImportFile | 認証済み |
| FILE-001 | ファイル一覧 | /FileManagement/Index | 認証済み |
| FILE-002 | ファイルアップロード | /FileManagement/Upload | 認証済み |
| MAIL-001 | メール送信フォーム | /MailSample/Index | 認証済み |
| MAIL-002 | メール送信完了 | /MailSample/Complete | 認証済み |
| MAIL-003 | メール送信ログ一覧 | /MailLog/Index | 認証済み |
| WIZ-001 | ウィザード Step 1 | /WizardSample/Step1 | 認証済み |
| WIZ-002 | ウィザード Step 2 | /WizardSample/Step2 | 認証済み |
| WIZ-003 | ウィザード確認 | /WizardSample/Confirm | 認証済み |
| WIZ-004 | ウィザード完了 | /WizardSample/Complete | 認証済み |
| DASH-001 | ダッシュボード | /Dashboard/Index | 認証済み |
| APR-001 | 承認申請一覧 | /ApprovalRequest/Index | 認証済み |
| APR-002 | 承認申請作成 | /ApprovalRequest/Create | 認証済み |
| APR-003 | 承認申請編集 | /ApprovalRequest/Edit/{id} | 認証済み |
| APR-004 | 承認申請詳細・承認/却下 | /ApprovalRequest/Detail/{id} | 認証済み |
| APR-005 | 承認申請削除確認 | /ApprovalRequest/Delete/{id} | 認証済み |
| SCH-001 | スケジュール（カレンダーメイン） | /Schedule/Index | 認証済み |
| SCH-002 | 予定作成フォーム（モーダル） | /Schedule/Create | 認証済み |
| SCH-003 | 予定編集フォーム（モーダル） | /Schedule/Edit/{id} | 認証済み |
| USR-001 | ユーザー管理一覧 | /UserManagement/Index | Admin |
| USR-002 | ユーザー情報編集 | /UserManagement/Edit | Admin |
| VIEW-001 | UI サンプル | /ViewSample | 認証済み |

---

## 8. 非機能要件

### セキュリティ

| 項目 | 設定・方針 |
|------|-----------|
| 認証方式 | Cookie 認証（ASP.NET Core Identity） |
| セッション有効期限 | 1440 分（24 時間） |
| Cookie 設定 | HttpOnly、Secure 属性付き |
| パスワードハッシュ | ASP.NET Core Identity の PBKDF2 ハッシュ |
| アカウントロック | 5 回失敗 → 5 分ロック |
| HTTPS | HTTPS リダイレクト強制 |
| ファイルアップロード | サイズ・拡張子チェック、GUID ベースのファイル名でパストラバーサル防止 |
| CSRF 対策 | ASP.NET Core 標準の Antiforgery トークン |
| SQL インジェクション対策 | EF Core パラメータバインディング |

### 可用性・拡張性

| 項目 | 方針 |
|------|------|
| スケーラビリティ | ステートレス設計（セッションはサーバー側管理、必要に応じて分散セッションへ移行可能） |
| データ保護 | ASP.NET Core Data Protection（ファイルベースのキー永続化） |
| バッチ実行 | Mutex による多重起動防止 |

### 運用・保守

| 項目 | 方針 |
|------|------|
| ログ | アクセスログ（AccessLogAttribute）＋ ASP.NET Core 標準ログ |
| 論理削除 | 全エンティティで DelFlag による論理削除（物理削除なし） |
| 履歴管理 | SampleEntity / SampleEntityChild / ScheduleEventEntity の更新履歴を History テーブルに保持 |
| テスト | xUnit / Moq によるユニットテスト |

---

## 9. 環境構成

### 前提条件

| 項目 | 要件 |
|------|------|
| .NET SDK | 10.0 以上 |
| データベース | SQL Server（localhost で起動済み） |
| SQL ユーザー | `admin` / `admin`（SQL Server 認証） |

### 接続設定

```json
{
  "ConnectionStrings": {
    "SiteConnection": "Server=localhost;Database=DevNextDB;Integrated Security=False;User ID=admin;Password=admin;TrustServerCertificate=True;"
  }
}
```

### 環境別設定

| 設定項目 | 開発環境 | 本番環境 |
|----------|---------|---------|
| ログレベル | Debug | Information |
| SMTP ホスト | localhost（smtp4dev） | smtp.example.com |
| SMTP ポート | 1025 | 25 |
| SSL | 無効 | 有効 |

### 起動手順

```bash
# 1. DB マイグレーション適用（初回のみ）
cd H:/ClaudeCode/DevNext && dotnet ef database update --project DevNext

# 2. Web アプリ起動（未適用マイグレーション・Seed は起動時に自動実行）
cd DevNext
dotnet run

# 3. テスト実行
cd Tests
dotnet test
```

### URL

| 環境 | URL |
|------|-----|
| 開発環境 | https://localhost:5001 |

### 初期ユーザー

| UserName | Password | Role |
|----------|----------|------|
| admin1@sample.jp | Admin1! | Admin |
| member1@sample.jp | Member1! | Member |

---

## 10. 外部インターフェース

### メールサーバー（SMTP）

| 項目 | 内容 |
|------|------|
| プロトコル | SMTP |
| 設定場所 | appsettings.json > Smtp セクション |
| 開発用ツール | smtp4dev（ローカル擬似 SMTP サーバー） |
| 接続方式 | TCP（設定により TLS 対応） |

### ファイルストレージ

| 項目 | 内容 |
|------|------|
| 種別 | ローカルファイルシステム |
| 保存先 | wwwroot/Uploads/ 配下 |
| ファイル名 | GUID ベース（衝突・パストラバーサル防止） |
| メタ情報 | FileEntity テーブルで DB 管理 |

---

*本ドキュメントは DevNext プロジェクトの基本設計を記述したものです。*
